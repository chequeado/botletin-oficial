# .env.example
# Copy to .env and fill in your values — never commit .env to git

# Required for the ETL prompt analysis
OPENAI_API_KEY=sk-...

# Required for email delivery (publisher/utils/mailing.py)
MAILCHIMP_API_KEY=SG...

# Optional: override SQLite path inside the container
# DATABASE_URL=sqlite:////app/data/boletinoficial.db
